<!--components/select-text/index.wxml-->
<!--选择插件-->
<view class="select-container">
    <text wx:for="{{list}}" class='select-text {{activeKey == item.value?"select-text_active":""}}' wx:key="value" bindtap="tapEvent" data-value="{{item.value}}">{{item.label}}</text>
</view>